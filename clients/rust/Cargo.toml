[package]
name = "zmr-client"
version = "0.1.0-dev.2"
edition = "2021"
license = "MIT"
description = "Rust JSON-RPC client for Zig Mobile Runner."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
